# Calibrate Icon & Favicon Setup

This document explains how favicons and app icons are configured for all Calibrate apps.

## ✅ Current Setup

All three apps (site, console, docs) use **Next.js 13+ App Router's dynamic icon generation** via `icon.tsx` and `apple-icon.tsx` files.

### How It Works

Next.js automatically generates favicons and app icons from these files:
- `app/icon.tsx` → Generates `/favicon.ico`, `/icon.png`, and other sizes
- `app/apple-icon.tsx` → Generates `/apple-touch-icon.png` (180×180)

### Icon Design

All icons use the new Calibrate brand colors:
- **Gradient Background**: `linear-gradient(90deg, #67C8FF 0%, #377BFF 50%, #0E3AFF 100%)`
- **Dial Icon**: Simplified 3-segment calibration dial in navy (`#001845`)
- **Rounded Corners**: 14px for favicon, 28px for Apple touch icon

### Files Updated

#### Site App (`apps/site/app/`)
- ✅ `icon.tsx` - Updated with new brand colors and dial icon
- ✅ `apple-icon.tsx` - Updated with new brand colors
- ✅ `opengraph-image.tsx` - Updated with new brand colors and tagline

#### Console App (`apps/console/app/`)
- ✅ `icon.tsx` - Updated with new brand colors and dial icon
- ✅ `apple-icon.tsx` - Created with new brand colors

#### Docs App (`apps/docs/app/`)
- ✅ `icon.tsx` - Created with new brand colors and dial icon
- ✅ `apple-icon.tsx` - Created with new brand colors

### Metadata Configuration

All apps have simplified metadata that relies on Next.js icon generation:

```typescript
export const metadata: Metadata = {
  themeColor: '#0E3AFF',
  // Icons are generated by icon.tsx and apple-icon.tsx files
  // Additional icons can be added to /branding/assets/ if needed
  manifest: '/branding/assets/manifest.webmanifest',
  // ...
}
```

## 🎨 Icon Specifications

### Favicon (icon.tsx)
- **Size**: 64×64px (Next.js generates multiple sizes automatically)
- **Format**: PNG (generated from React component)
- **Background**: Blue gradient (L1 → L2 → L3)
- **Icon**: Calibration dial in navy

### Apple Touch Icon (apple-icon.tsx)
- **Size**: 180×180px
- **Format**: PNG
- **Background**: Blue gradient
- **Icon**: Calibration dial in navy
- **Border Radius**: 28px (iOS requirement)

### OpenGraph Image (opengraph-image.tsx - site only)
- **Size**: 1200×630px
- **Format**: PNG
- **Background**: Dark navy with blue gradient accents
- **Content**: Calibrate logo + tagline

## 📁 Generated Files

When the apps are built, Next.js automatically generates:

```
/public/
  ├── favicon.ico          (from icon.tsx)
  ├── icon.png            (from icon.tsx)
  ├── icon-192.png        (from icon.tsx)
  ├── icon-512.png        (from icon.tsx)
  └── apple-touch-icon.png (from apple-icon.tsx)
```

These files are generated at build time and don't need to be committed to git.

## 🔧 Testing Icons

To verify icons are working:

1. **Development**: Run `pnpm dev` and check:
   - Browser tab shows favicon
   - `/favicon.ico` returns 200
   - `/icon.png` returns 200

2. **Production Build**: Run `pnpm build` and check:
   - Generated files in `.next/static/` or `out/`
   - All icon sizes are generated

3. **Browser DevTools**:
   - Check Network tab for icon requests
   - Verify correct sizes are loaded
   - Check Application tab → Manifest for PWA icons

## 📝 Optional: Static Icon Files

If you need static icon files (e.g., for external services, email templates), you can:

1. Export icons from the `icon.tsx` components
2. Place them in `/public/branding/assets/`:
   - `favicon.ico` (32×32)
   - `icon-512.png` (512×512)
   - `maskable.png` (192×192 for PWA)
   - `og-image.png` (1200×630)

3. Update metadata to reference these files if needed

## 🚀 Next Steps

The icon system is fully functional. Next.js will:
- ✅ Generate all required icon sizes automatically
- ✅ Serve icons at the correct paths
- ✅ Use the new brand colors consistently
- ✅ Work across all browsers and devices

No additional setup required!

---

**Last Updated**: January 2025  
**Branding Version**: v1

