{
  "buildCommand": "cd apps/site && pnpm install --frozen-lockfile && pnpm build",
  "outputDirectory": "apps/site/.next",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile"
}
