{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && cd packages/db && pnpm install && pnpm exec prisma generate && cd ../.. && pnpm --filter @calibr/console build",
  "installCommand": "corepack prepare pnpm@9.0.0 --activate && cd ../.. && pnpm install --frozen-lockfile=false --shamefully-hoist",
  "outputDirectory": ".next"
}
