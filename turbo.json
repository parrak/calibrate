{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build", "@calibr/db#generate"],
      "outputs": ["dist/**", ".next/**", "**/*.html", "**/*.yaml"]
    },
    "dev": { "cache": false },
    "lint": {},
    "test": {},
    "typecheck": {},
    "generate": {
      "cache": false
    }
  }
}
