{
  "name": "@calibr/db",
  "version": "0.0.1",
  "type": "module",
  "main": "index.js",
  "prisma": {
    "schema": "prisma/schema.prisma",
    "seed": "tsx prisma/seed.ts"
  },
  "scripts": {
    "prisma": "prisma",
    "migrate": "prisma migrate dev",
    "migrate:check": "tsx scripts/check-migrations.ts",
    "migrate:resolve-init": "tsx scripts/resolve-init-migration.ts",
    "generate": "prisma generate",
    "generate:schemas": "tsx scripts/generate-schemas.ts",
    "generate:dtos": "tsx scripts/generate-dtos.ts",
    "generate:all": "pnpm generate && pnpm generate:schemas && pnpm generate:dtos",
    "studio": "prisma studio",
    "seed": "tsx prisma/seed.ts",
    "test:db": "tsx scripts/test-db.ts"
  },
  "dependencies": {
    "@calibr/security": "workspace:*",
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "prisma": "^5.22.0",
    "tsx": "^4.21.0"
  }
}
