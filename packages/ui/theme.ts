/**
 * Shared theme tokens for Calibrate UI
 * These tokens ensure consistent styling across marketing site and console
 */

export const theme = {
  colors: {
    bg: 'var(--bg)',
    surface: 'var(--surface)',
    border: 'var(--border)',
    fg: 'var(--fg)',
    mute: 'var(--mute)',
    brand: 'var(--brand)',
  },
  radius: {
    xl: '0.75rem',
    '2xl': '1rem',
  },
}

/**
 * CSS custom properties should be defined in globals.css:
 * :root {
 *   --bg: #0b0b0c;
 *   --surface: #151517;
 *   --border: #202124;
 *   --fg: #e5e7eb;
 *   --mute: #9ca3af;
 *   --brand: #00c2a8;
 * }
 */

